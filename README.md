# Ruby on Rails Docker Starter

Based on [this article](https://docs.docker.com/compose/rails/#define-the-project)

To build the project do:

```docker-compose run web rails new . --force --database=postgresql --skip-bundle
```
